/**
 * Test database configuration
 *
 * Integration tests use a separate test database.
 * Set DATABASE_URL_TEST in your .env.test file.
 *
 * Run: bun run db:setup-test to initialize the test database.
 */
export {}

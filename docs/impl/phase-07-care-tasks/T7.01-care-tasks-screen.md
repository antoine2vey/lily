# T7.01 - Care Tasks Screen Shell

Main care tasks screen with header.

## Status: Not Started

## Depends On
- T1.04 (Care Tab)

## Design Reference
- `today's_care_tasks`

## Output Files
- `packages/app/src/screens/care/CareScreen.tsx`
- `packages/app/src/screens/care/__tests__/CareScreen.test.tsx`

## Implementation
```typescript
export function CareScreen() {
  const { data: tasks, isLoading } = useCareTasks()
  const today = new Date()

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.date}>{formatDate(today, 'TODAY, MMM D')}</Text>
        <Text style={styles.title}>Care</Text>
        <Pressable style={styles.calendarButton}>
          <CalendarIcon size={24} color="#1A1A1A" />
        </Pressable>
      </View>
      <ScrollView>
        {/* Task sections */}
      </ScrollView>
    </SafeAreaView>
  )
}
```

## Visual Specs
- Date: 12px, uppercase, #757575
- Title: "Care", 28px, bold
- Calendar icon: top right

## Test Cases
```typescript
describe('CareScreen', () => {
  it('renders today date')
  it('renders "Care" title')
  it('renders calendar button')
  it('fetches care tasks')
  it('shows loading state')
})
```

## Completion Checklist
- [ ] Header with date and title
- [ ] Calendar button renders
- [ ] Tasks fetched
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add care tasks screen shell
```

# T7.03 - Today Section

Section showing today's care tasks.

## Status: Not Started

## Depends On
- T7.01 (Care Tasks Screen)

## Design Reference
- `today's_care_tasks` (Today section)

## Output Files
- `packages/app/src/screens/care/components/TodaySection.tsx`
- `packages/app/src/screens/care/components/__tests__/TodaySection.test.tsx`

## Implementation
```typescript
interface TodaySectionProps {
  tasks: CareTask[]
  onTaskPress: (task: CareTask) => void
  onTaskComplete: (taskId: string) => void
}

export function TodaySection({ tasks, onTaskPress, onTaskComplete }: TodaySectionProps) {
  return (
    <View style={styles.section}>
      <Text style={styles.title}>Today</Text>
      {tasks.length === 0 ? (
        <Text style={styles.emptyText}>No tasks for today!</Text>
      ) : (
        tasks.map((task) => (
          <CareTaskCard
            key={task.id}
            task={task}
            onPress={() => onTaskPress(task)}
            onComplete={() => onTaskComplete(task.id)}
          />
        ))
      )}
    </View>
  )
}
```

## Visual Specs
- Title: "Today", 18px, semibold
- Empty message: centered, gray text
- Task cards: standard styling with checkmark

## Test Cases
```typescript
describe('TodaySection', () => {
  it('renders section title')
  it('renders task cards')
  it('shows empty message when no tasks')
  it('marks task complete on check')
})
```

## Completion Checklist
- [ ] Title renders
- [ ] Tasks display
- [ ] Empty state works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add today section to care tasks
```

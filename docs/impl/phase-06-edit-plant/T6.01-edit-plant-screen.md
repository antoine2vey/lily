# T6.01 - Edit Plant Screen Shell

Edit plant screen with form layout.

## Status: Not Started

## Depends On
- T4.09 (Edit Navigation)

## Design Reference
- `edit_plant_details`

## Output Files
- `packages/app/src/screens/edit-plant/EditPlantScreen.tsx`
- `packages/app/src/screens/edit-plant/__tests__/EditPlantScreen.test.tsx`

## Implementation
```typescript
export function EditPlantScreen() {
  const { plantId } = useRoute<EditPlantRoute>().params
  const { data: plant, isLoading } = usePlant(plantId)
  const navigation = useNavigation()

  if (isLoading) return <LoadingScreen />

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <Pressable onPress={navigation.goBack}>
          <Text style={styles.cancelText}>Cancel</Text>
        </Pressable>
        <Text style={styles.title}>Edit Plant</Text>
        <Pressable onPress={handleSave}>
          <Text style={styles.saveText}>Save</Text>
        </Pressable>
      </View>
      <ScrollView style={styles.content}>
        {/* Form fields added in subsequent tasks */}
      </ScrollView>
    </SafeAreaView>
  )
}
```

## Visual Specs
- Header: Cancel (left), "Edit Plant" (center), Save (right)
- Cancel: 16px, regular, #757575
- Save: 16px, medium, #5B8C5A
- Content: white background, scrollable

## Test Cases
```typescript
describe('EditPlantScreen', () => {
  it('fetches plant by id')
  it('shows loading while fetching')
  it('renders Cancel and Save buttons')
  it('goes back on Cancel')
})
```

## Completion Checklist
- [ ] Header with Cancel/Save
- [ ] Plant data fetched
- [ ] Loading state
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add edit plant screen shell
```

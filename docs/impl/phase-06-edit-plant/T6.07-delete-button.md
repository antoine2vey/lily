# T6.07 - Delete Plant Button

Destructive delete button at bottom.

## Status: Not Started

## Depends On
- T6.01 (Edit Plant Screen)

## Design Reference
- `edit_plant_details` (Delete Plant button)

## Output Files
- Update `packages/app/src/screens/edit-plant/EditPlantScreen.tsx`

## Implementation
```typescript
const [showDeleteConfirm, setShowDeleteConfirm] = useState(false)

<Pressable onPress={() => setShowDeleteConfirm(true)} style={styles.deleteButton}>
  <Text style={styles.deleteText}>Delete Plant</Text>
</Pressable>

<ConfirmationModal
  visible={showDeleteConfirm}
  title={`Delete ${plant.name}?`}
  message="This will permanently remove all care history and photos."
  confirmLabel="Delete Plant"
  cancelLabel="Keep Plant"
  destructive
  onConfirm={handleDelete}
  onCancel={() => setShowDeleteConfirm(false)}
/>
```

## Visual Specs
- Button: full width, transparent background
- Text: 16px, #E8997E (coral), centered
- Position: bottom of scroll content, with margin

## Test Cases
```typescript
describe('Delete Button', () => {
  it('renders delete button')
  it('shows confirmation modal on press')
  it('deletes plant on confirm')
  it('closes modal on cancel')
})
```

## Completion Checklist
- [ ] Delete button renders in coral
- [ ] Confirmation modal appears
- [ ] Delete works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add delete button to edit plant
```

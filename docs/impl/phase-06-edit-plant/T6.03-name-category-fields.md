# T6.03 - Name and Category Fields

Editable name and category inputs.

## Status: Not Started

## Depends On
- T6.01 (Edit Plant Screen)

## Design Reference
- `edit_plant_details` (PLANT NAME, CATEGORY fields)

## Output Files
- Update `packages/app/src/screens/edit-plant/EditPlantScreen.tsx`

## Implementation
```typescript
const [name, setName] = useState(plant.name)
const [category, setCategory] = useState(plant.category)

<Input
  label="PLANT NAME"
  value={name}
  onChangeText={setName}
  placeholder="e.g. Swiss Cheese Plant"
/>

<Select
  label="CATEGORY"
  value={category}
  onValueChange={setCategory}
  options={[
    { label: 'Tropical', value: 'tropical' },
    { label: 'Succulent', value: 'succulent' },
    { label: 'Flowering', value: 'flowering' },
    { label: 'Foliage', value: 'foliage' },
    { label: 'Herb', value: 'herb' },
    { label: 'Other', value: 'other' },
  ]}
/>
```

## Visual Specs
- Label: 12px, uppercase, #757575
- Input: 16px, #1A1A1A, light green background
- Category: dropdown with chevron

## Test Cases
```typescript
describe('Name and Category Fields', () => {
  it('pre-fills with current plant name')
  it('pre-fills with current category')
  it('allows editing name')
  it('allows changing category')
})
```

## Completion Checklist
- [ ] Name field editable
- [ ] Category dropdown works
- [ ] Pre-filled with current values
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add name and category fields to edit
```

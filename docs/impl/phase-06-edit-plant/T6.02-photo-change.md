# T6.02 - Photo Change Button

Change plant photo from edit screen.

## Status: Not Started

## Depends On
- T6.01 (Edit Plant Screen)

## Design Reference
- `edit_plant_details` (photo with edit icon)

## Output Files
- Update `packages/app/src/screens/edit-plant/EditPlantScreen.tsx`

## Implementation
```typescript
const [photo, setPhoto] = useState(plant.imageUrl)

const handleChangePhoto = async () => {
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    allowsEditing: true,
    aspect: [1, 1],
    quality: 0.8,
  })

  if (!result.canceled) {
    setPhoto(result.assets[0].uri)
  }
}

<View style={styles.photoContainer}>
  <Image source={{ uri: photo }} style={styles.photo} />
  <Pressable onPress={handleChangePhoto} style={styles.editPhotoButton}>
    <EditIcon size={20} color="#FFF" />
  </Pressable>
  <Text style={styles.changePhotoText}>Change Photo</Text>
</View>
```

## Visual Specs
- Photo: 120px rounded square, centered
- Edit button: 32px circle, green, bottom-right of photo
- "Change Photo" text: green, below photo

## Test Cases
```typescript
describe('Photo Change', () => {
  it('displays current plant photo')
  it('opens image picker on press')
  it('updates preview after selection')
  it('shows edit icon overlay')
})
```

## Completion Checklist
- [ ] Current photo displays
- [ ] Image picker opens
- [ ] Preview updates
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add photo change to edit plant
```

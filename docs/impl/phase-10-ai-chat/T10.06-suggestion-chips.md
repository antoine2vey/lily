# T10.06 - Suggestion Chips

Scrollable suggestion prompts.

## Status: Not Started

## Depends On
- T0.10 (Chip)
- T10.01 (Chat Screen)

## Design Reference
- `ai_plant_assistant_chat` (bottom suggestion chips)

## Output Files
- `packages/app/src/screens/chat/components/SuggestionChips.tsx`

## Implementation
```typescript
const SUGGESTIONS = [
  'Why are my leaves yellowing?',
  'How often should I water?',
  'Best fertilizer to use?',
  'Is this plant pet safe?',
]

interface SuggestionChipsProps {
  onSelect: (suggestion: string) => void
  plantName?: string
}

export function SuggestionChips({ onSelect, plantName }: SuggestionChipsProps) {
  const suggestions = plantName
    ? SUGGESTIONS.map(s => s.replace('my', `my ${plantName}'s`))
    : SUGGESTIONS

  return (
    <ScrollView
      horizontal
      showsHorizontalScrollIndicator={false}
      style={styles.container}
      contentContainerStyle={styles.content}
    >
      {suggestions.map((suggestion) => (
        <Chip
          key={suggestion}
          label={suggestion}
          onPress={() => onSelect(suggestion)}
          variant="suggestion"
        />
      ))}
    </ScrollView>
  )
}
```

## Visual Specs
- Horizontal scroll
- Chips: outline style, question icon prefix
- Padding: 16px horizontal

## Test Cases
```typescript
describe('SuggestionChips', () => {
  it('renders suggestion chips')
  it('scrolls horizontally')
  it('calls onSelect with suggestion text')
  it('customizes suggestions with plant name')
})
```

## Completion Checklist
- [ ] Chips render
- [ ] Horizontal scroll
- [ ] Selection works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add suggestion chips to chat
```

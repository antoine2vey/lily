# T10.11 - Chat Navigation from Plant Detail

Navigate to chat from plant detail.

## Status: Not Started

## Depends On
- T4.03 (Quick Actions)
- T10.01 (Chat Screen)

## Design Reference
- `plant_detail_view` (Chat button)

## Output Files
- Update `packages/app/src/screens/plant-detail/PlantDetailScreen.tsx`

## Implementation
```typescript
const navigation = useNavigation()

const handleChat = () => {
  navigation.navigate('Chat', { plantId: plant.id })
}

<QuickActions
  // ...
  onChat={handleChat}
/>
```

## Navigation Setup
```typescript
type RootStackParamList = {
  // ...
  Chat: { plantId?: string }
}
```

## Test Cases
```typescript
describe('Chat Navigation', () => {
  it('navigates to Chat screen')
  it('passes plantId as param')
  it('chat loads with plant context')
})
```

## Completion Checklist
- [ ] Chat button navigates
- [ ] Plant ID passed
- [ ] Chat has plant context
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add chat navigation from plant detail
```

# T10.01 - Chat Screen Shell

AI chat screen with message list and input.

## Status: Not Started

## Depends On
- T4.03 (Chat quick action)

## Design Reference
- `ai_plant_assistant_chat`

## Output Files
- `packages/app/src/screens/chat/ChatScreen.tsx`
- `packages/app/src/screens/chat/__tests__/ChatScreen.test.tsx`

## Implementation
```typescript
export function ChatScreen() {
  const { plantId } = useRoute<ChatRoute>().params ?? {}
  const { data: messages, isLoading } = useChatHistory(plantId)
  const flatListRef = useRef<FlatList>(null)

  return (
    <SafeAreaView style={styles.container}>
      <ChatHeader />
      <FlatList
        ref={flatListRef}
        data={messages}
        renderItem={({ item }) => <ChatMessage message={item} />}
        keyExtractor={(item) => item.id}
        inverted
        contentContainerStyle={styles.messageList}
      />
      <SuggestionChips plantId={plantId} />
      <ChatInput onSend={handleSend} />
    </SafeAreaView>
  )
}
```

## Visual Specs
- Background: #F8FAF8
- Messages: inverted list (newest at bottom)
- Input: sticky at bottom

## Test Cases
```typescript
describe('ChatScreen', () => {
  it('renders chat header')
  it('fetches message history')
  it('renders messages in list')
  it('renders input at bottom')
})
```

## Completion Checklist
- [ ] Header renders
- [ ] Messages load
- [ ] Input renders
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add chat screen shell
```

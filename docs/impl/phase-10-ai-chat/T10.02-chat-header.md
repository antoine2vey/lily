# T10.02 - Chat Header

Header with assistant avatar and status.

## Status: Not Started

## Depends On
- T0.09 (Avatar)
- T10.01 (Chat Screen)

## Design Reference
- `ai_plant_assistant_chat` (header)

## Output Files
- `packages/app/src/screens/chat/components/ChatHeader.tsx`

## Implementation
```typescript
export function ChatHeader() {
  const navigation = useNavigation()

  return (
    <View style={styles.header}>
      <Pressable onPress={navigation.goBack} style={styles.backButton}>
        <ChevronLeftIcon size={24} color="#1A1A1A" />
      </Pressable>
      <Avatar source={require('@/assets/lily-avatar.png')} size="md" />
      <View style={styles.info}>
        <Text style={styles.name}>Lily Assistant</Text>
        <View style={styles.statusRow}>
          <View style={styles.statusDot} />
          <Text style={styles.statusText}>Online</Text>
        </View>
      </View>
      <Pressable style={styles.menuButton}>
        <MoreVerticalIcon size={24} color="#1A1A1A" />
      </Pressable>
    </View>
  )
}
```

## Visual Specs
- Avatar: 40px, Lily mascot image
- Name: "Lily Assistant", 16px, semibold
- Status: green dot + "Online", 12px, #5B8C5A

## Test Cases
```typescript
describe('ChatHeader', () => {
  it('renders avatar')
  it('renders "Lily Assistant" name')
  it('shows online status')
  it('back button navigates back')
})
```

## Completion Checklist
- [ ] Avatar displays
- [ ] Name and status show
- [ ] Back navigation works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add chat header component
```

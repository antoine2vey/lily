# T10.09 - Image in Chat Message

Display attached image in chat.

## Status: Not Started

## Depends On
- T10.04 (User Message Bubble)
- T10.08 (Photo Attachment)

## Design Reference
- `ai_chat_image_attachment` (image in message)

## Output Files
- Update `packages/app/src/screens/chat/components/UserMessageBubble.tsx`

## Implementation
```typescript
// In UserMessageBubble
{message.imageUrl && (
  <Pressable onPress={() => openImageViewer(message.imageUrl)}>
    <Image source={{ uri: message.imageUrl }} style={styles.attachedImage} />
    <View style={styles.imageOverlay}>
      <Text style={styles.imageLabel}>Image attached</Text>
      <Text style={styles.analysisStatus}>✨ Analysis ready</Text>
    </View>
  </Pressable>
)}
```

## Visual Specs
- Image: 200px max width, rounded corners
- Overlay: subtle gradient at bottom
- Label: "Image attached"
- Status: sparkle + "Analysis ready"

## Test Cases
```typescript
describe('Image in Chat', () => {
  it('renders attached image')
  it('shows image label')
  it('shows analysis status')
  it('opens viewer on tap')
})
```

## Completion Checklist
- [ ] Image renders
- [ ] Overlay shows
- [ ] Tap opens viewer
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add image display in chat messages
```

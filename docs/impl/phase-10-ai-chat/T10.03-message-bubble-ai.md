# T10.03 - Message Bubble (AI)

AI/assistant message bubble component.

## Status: Not Started

## Depends On
- T10.01 (Chat Screen)

## Design Reference
- `ai_plant_assistant_chat` (green bubbles on left)

## Output Files
- `packages/app/src/screens/chat/components/AIMessageBubble.tsx`

## Implementation
```typescript
interface AIMessageBubbleProps {
  message: {
    content: string
    timestamp: Date
  }
}

export function AIMessageBubble({ message }: AIMessageBubbleProps) {
  return (
    <View style={styles.container}>
      <Avatar source={require('@/assets/lily-avatar.png')} size="sm" />
      <View style={styles.bubbleContainer}>
        <View style={styles.bubble}>
          <Text style={styles.content}>{message.content}</Text>
        </View>
        <Text style={styles.timestamp}>{formatTime(message.timestamp)}</Text>
      </View>
    </View>
  )
}
```

## Visual Specs
- Container: left-aligned, avatar on left
- Bubble: #E8F5E8 background, 16px border-radius
- Content: 14px, #1A1A1A
- Timestamp: 12px, #9E9E9E, below bubble

## Test Cases
```typescript
describe('AIMessageBubble', () => {
  it('renders avatar on left')
  it('renders message content')
  it('renders formatted timestamp')
  it('applies correct bubble styling')
})
```

## Completion Checklist
- [ ] Avatar shows
- [ ] Bubble styled correctly
- [ ] Content displays
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add AI message bubble component
```

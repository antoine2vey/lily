# T10.08 - Photo Attachment Button

Attach photo to chat message.

## Status: Not Started

## Depends On
- T10.07 (Chat Input)

## Design Reference
- `ai_chat_image_attachment`

## Output Files
- Update `packages/app/src/screens/chat/ChatScreen.tsx`
- `packages/app/src/screens/chat/components/PhotoPreview.tsx`

## Implementation
```typescript
const [pendingPhoto, setPendingPhoto] = useState<string | null>(null)

const handleAttachPhoto = async () => {
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    allowsEditing: true,
    quality: 0.8,
  })

  if (!result.canceled) {
    setPendingPhoto(result.assets[0].uri)
  }
}

{pendingPhoto && (
  <PhotoPreview
    uri={pendingPhoto}
    onRemove={() => setPendingPhoto(null)}
    analyzing={isAnalyzing}
  />
)}
```

## Visual Specs
- Preview: thumbnail above input, 60px
- Remove X: top-right of preview
- "Analysis ready" badge when processed

## Test Cases
```typescript
describe('Photo Attachment', () => {
  it('opens image picker')
  it('shows preview after selection')
  it('removes photo when X pressed')
  it('includes photo in message')
})
```

## Completion Checklist
- [ ] Picker opens
- [ ] Preview shows
- [ ] Remove works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add photo attachment to chat
```

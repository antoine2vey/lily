# T10.04 - Message Bubble (User)

User message bubble component.

## Status: Not Started

## Depends On
- T10.01 (Chat Screen)

## Design Reference
- `ai_plant_assistant_chat` (gray bubbles on right)

## Output Files
- `packages/app/src/screens/chat/components/UserMessageBubble.tsx`

## Implementation
```typescript
interface UserMessageBubbleProps {
  message: {
    content: string
    timestamp: Date
    imageUrl?: string
  }
}

export function UserMessageBubble({ message }: UserMessageBubbleProps) {
  return (
    <View style={styles.container}>
      <View style={styles.bubbleContainer}>
        {message.imageUrl && (
          <Image source={{ uri: message.imageUrl }} style={styles.image} />
        )}
        <View style={styles.bubble}>
          <Text style={styles.content}>{message.content}</Text>
        </View>
        <Text style={styles.timestamp}>{formatTime(message.timestamp)}</Text>
      </View>
    </View>
  )
}
```

## Visual Specs
- Container: right-aligned
- Bubble: #F5F5F5 background, 16px border-radius
- Content: 14px, #1A1A1A
- Timestamp: 12px, #9E9E9E, right-aligned

## Test Cases
```typescript
describe('UserMessageBubble', () => {
  it('renders message content')
  it('renders formatted timestamp')
  it('right-aligns bubble')
  it('renders image when provided')
})
```

## Completion Checklist
- [ ] Right-aligned
- [ ] Bubble styled correctly
- [ ] Image shows when present
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add user message bubble component
```

# T8.06 - Add Care Log FAB

FAB that opens log care entry sheet.

## Status: Not Started

## Depends On
- T8.01 (Care History Screen)

## Design Reference
- `care_history_timeline` (+ FAB)

## Output Files
- Update `packages/app/src/screens/care-history/CareHistoryScreen.tsx`

## Implementation
```typescript
const [showLogCare, setShowLogCare] = useState(false)

<FAB
  icon={<PlusIcon size={24} color="#FFF" />}
  onPress={() => setShowLogCare(true)}
  style={styles.fab}
/>

<LogCareSheet
  visible={showLogCare}
  onClose={() => setShowLogCare(false)}
  defaultPlantId={plantId}
/>
```

## Visual Specs
- FAB: 56px circle, #5B8C5A, bottom-right
- Icon: white +, 24px
- Shadow: elevation 4

## Test Cases
```typescript
describe('Add Care Log FAB', () => {
  it('renders FAB in bottom right')
  it('opens LogCareSheet when pressed')
  it('pre-selects current plant')
})
```

## Completion Checklist
- [ ] FAB renders
- [ ] Opens log care sheet
- [ ] Plant pre-selected
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add FAB to care history screen
```

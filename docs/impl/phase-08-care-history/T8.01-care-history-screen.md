# T8.01 - Care History Screen Shell

Timeline screen for plant care history.

## Status: Not Started

## Depends On
- T4.07 (Recent History navigation)

## Design Reference
- `care_history_timeline`

## Output Files
- `packages/app/src/screens/care-history/CareHistoryScreen.tsx`
- `packages/app/src/screens/care-history/__tests__/CareHistoryScreen.test.tsx`

## Implementation
```typescript
export function CareHistoryScreen() {
  const { plantId } = useRoute<CareHistoryRoute>().params
  const { data: plant } = usePlant(plantId)
  const { data: logs, isLoading } = useCareHistory(plantId)

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <BackButton />
        <View style={styles.titleContainer}>
          <Text style={styles.title}>Care History</Text>
          <Text style={styles.subtitle}>{plant?.name}</Text>
        </View>
        <FilterButton onPress={showFilterSheet} />
      </View>
      <ScrollView>
        {/* Timeline content */}
      </ScrollView>
      <FAB icon={<PlusIcon />} onPress={showLogCareSheet} />
    </SafeAreaView>
  )
}
```

## Visual Specs
- Header: "Care History" title, plant name subtitle
- Filter icon: top right
- FAB: bottom right, green, + icon

## Test Cases
```typescript
describe('CareHistoryScreen', () => {
  it('renders header with plant name')
  it('fetches care history for plant')
  it('shows loading state')
  it('renders FAB for adding logs')
})
```

## Completion Checklist
- [ ] Header with plant name
- [ ] History data fetched
- [ ] FAB renders
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add care history screen shell
```

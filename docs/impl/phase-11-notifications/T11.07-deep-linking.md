# T11.07 - Notification Deep Linking

Navigate to relevant screen on tap.

## Status: Not Started

## Depends On
- T11.03 (Notification Item)

## Design Reference
- `notification_center` (tap → relevant screen)

## Output Files
- Update NotificationItem and NotificationsScreen

## Implementation
```typescript
const navigation = useNavigation()

const handleNotificationPress = (notification: Notification) => {
  // Mark as read
  markAsRead(notification.id)

  // Navigate based on type
  switch (notification.type) {
    case 'care_reminder':
      navigation.navigate('PlantDetail', { plantId: notification.data.plantId })
      break
    case 'achievement':
      navigation.navigate('Achievements')
      break
    case 'tip':
      // Just mark read, no navigation
      break
    default:
      break
  }
}
```

## Test Cases
```typescript
describe('Notification Deep Linking', () => {
  it('navigates to plant detail for care reminders')
  it('navigates to achievements for achievement notifications')
  it('marks notification read on tap')
})
```

## Completion Checklist
- [ ] Care reminder → plant detail
- [ ] Achievement → achievements
- [ ] Mark read on tap
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add notification deep linking
```

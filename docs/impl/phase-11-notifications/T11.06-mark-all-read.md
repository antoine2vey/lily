# T11.06 - Mark All as Read

Mark all notifications read.

## Status: Not Started

## Depends On
- T11.01 (Notifications Screen)

## Design Reference
- `notification_center` (Mark all read action)

## Output Files
- Update `packages/app/src/screens/notifications/NotificationsScreen.tsx`

## Implementation
```typescript
const { mutate: markAllRead, isLoading } = useMarkAllNotificationsRead()

const handleMarkAllRead = () => {
  markAllRead()
}

// Hook
export function useMarkAllNotificationsRead() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async () => {
      await api.notifications.markAllRead()
    },
    onMutate: async () => {
      await queryClient.cancelQueries(['notifications'])
      const previous = queryClient.getQueryData(['notifications'])

      queryClient.setQueryData(['notifications'], (old: Notification[]) =>
        old.map((n) => ({ ...n, read: true }))
      )

      return { previous }
    },
    onSettled: () => {
      queryClient.invalidateQueries(['notifications'])
      queryClient.invalidateQueries(['unreadCount'])
    },
  })
}
```

## Test Cases
```typescript
describe('Mark All Read', () => {
  it('calls API')
  it('marks all as read optimistically')
  it('updates unread count')
})
```

## Completion Checklist
- [ ] All marked read
- [ ] Optimistic update
- [ ] Count updates
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add mark all notifications read
```

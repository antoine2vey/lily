# T11.02 - Tab Bar (All / Unread)

Tab bar to filter notifications.

## Status: Not Started

## Depends On
- T11.01 (Notifications Screen)

## Design Reference
- `notification_center` (All | Unread tabs)

## Output Files
- `packages/app/src/screens/notifications/components/NotificationTabs.tsx`

## Implementation
```typescript
interface NotificationTabsProps {
  activeTab: 'all' | 'unread'
  onTabChange: (tab: 'all' | 'unread') => void
  unreadCount: number
}

export function NotificationTabs({ activeTab, onTabChange, unreadCount }: NotificationTabsProps) {
  return (
    <View style={styles.container}>
      <Pressable
        onPress={() => onTabChange('all')}
        style={[styles.tab, activeTab === 'all' && styles.activeTab]}
      >
        <Text style={[styles.tabText, activeTab === 'all' && styles.activeTabText]}>All</Text>
      </Pressable>
      <Pressable
        onPress={() => onTabChange('unread')}
        style={[styles.tab, activeTab === 'unread' && styles.activeTab]}
      >
        <Text style={[styles.tabText, activeTab === 'unread' && styles.activeTabText]}>
          Unread ({unreadCount})
        </Text>
      </Pressable>
    </View>
  )
}
```

## Visual Specs
- Active tab: green underline, green text
- Inactive: gray text
- Unread shows count badge

## Test Cases
```typescript
describe('NotificationTabs', () => {
  it('renders both tabs')
  it('highlights active tab')
  it('shows unread count')
  it('calls onTabChange when pressed')
})
```

## Completion Checklist
- [ ] Tabs render
- [ ] Active state works
- [ ] Count shows
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add notification tabs
```

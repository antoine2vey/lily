# T14.07 - Support Section

Help Center and Contact Us rows.

## Status: Not Started

## Depends On
- T0.06 (ListRow)
- T14.01 (Settings Screen)

## Design Reference
- `general_app_settings` (Support section)

## Output Files
- Update SettingsScreen

## Implementation
```typescript
<View style={styles.section}>
  <Text style={styles.sectionTitle}>Support</Text>
  <ListRow
    leftIcon={<HelpCircleIcon />}
    title="Help Center"
    showChevron
    onPress={() => Linking.openURL('https://lily.app/help')}
  />
  <ListRow
    leftIcon={<MessageCircleIcon />}
    title="Contact Us"
    showChevron
    onPress={() => Linking.openURL('mailto:support@lily.app')}
  />
  <ListRow
    leftIcon={<InfoIcon />}
    title="About Lily"
    showChevron
    onPress={() => navigation.navigate('About')}
  />
</View>
```

## Test Cases
```typescript
describe('Support Section', () => {
  it('renders all support rows')
  it('opens help center URL')
  it('opens email for contact')
  it('navigates to About')
})
```

## Completion Checklist
- [ ] All rows render
- [ ] Links work
- [ ] Navigation works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add support section to settings
```

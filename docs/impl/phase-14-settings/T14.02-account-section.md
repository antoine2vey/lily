# T14.02 - Account Section

Email, password, subscription rows.

## Status: Not Started

## Depends On
- T0.06 (ListRow)
- T14.01 (Settings Screen)

## Design Reference
- `general_app_settings` (Account section)

## Output Files
- Update SettingsScreen

## Implementation
```typescript
<View style={styles.section}>
  <Text style={styles.sectionTitle}>Account</Text>
  <ListRow
    leftIcon={<MailIcon />}
    title="Email"
    subtitle={user.email}
    showChevron
    onPress={() => navigation.navigate('ChangeEmail')}
  />
  <ListRow
    leftIcon={<LockIcon />}
    title="Password"
    subtitle="Last changed 3 months ago"
    showChevron
    onPress={() => navigation.navigate('ChangePassword')}
  />
  <ListRow
    leftIcon={<CrownIcon />}
    title="Subscription"
    rightElement={<Badge label="Pro" variant="success" />}
    showChevron
    onPress={() => navigation.navigate('Subscription')}
  />
</View>
```

## Visual Specs
- Section title: 18px, semibold
- Rows: standard ListRow styling
- Subscription: badge showing current plan

## Test Cases
```typescript
describe('Account Section', () => {
  it('shows user email')
  it('shows subscription badge')
  it('navigates on row press')
})
```

## Completion Checklist
- [ ] All rows render
- [ ] User data shows
- [ ] Navigation works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add account section to settings
```

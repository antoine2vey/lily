# T14.01 - Settings Screen Shell

Main settings/profile screen.

## Status: Not Started

## Depends On
- T1.05 (Profile Tab)

## Design Reference
- `general_app_settings`

## Output Files
- `packages/app/src/screens/settings/SettingsScreen.tsx`
- `packages/app/src/screens/settings/__tests__/SettingsScreen.test.tsx`

## Implementation
```typescript
export function SettingsScreen() {
  const { user, logout } = useAuth()

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.title}>Settings</Text>
      </View>
      <ScrollView>
        {/* Settings sections */}
      </ScrollView>
      <Text style={styles.version}>Version 1.0.0</Text>
    </SafeAreaView>
  )
}
```

## Visual Specs
- Header: "Settings" title
- Sections: Account, Appearance, Notifications, Support
- Version: bottom, centered, gray

## Test Cases
```typescript
describe('SettingsScreen', () => {
  it('renders header')
  it('renders all sections')
  it('shows version number')
})
```

## Completion Checklist
- [ ] Header renders
- [ ] Sections display
- [ ] Version shows
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add settings screen shell
```

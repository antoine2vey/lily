# T14.04 - Appearance Section

Theme row that opens theme modal.

## Status: Not Started

## Depends On
- T0.06 (ListRow)
- T14.01 (Settings Screen)

## Design Reference
- `general_app_settings` (Appearance section)

## Output Files
- Update SettingsScreen

## Implementation
```typescript
const [showThemeModal, setShowThemeModal] = useState(false)
const { theme } = useTheme()

const getThemeLabel = () => {
  if (theme === 'system') return 'System'
  if (theme === 'dark') return 'Dark'
  return 'Light'
}

<View style={styles.section}>
  <Text style={styles.sectionTitle}>Appearance</Text>
  <ListRow
    leftIcon={<SunMoonIcon />}
    title="Theme"
    subtitle={getThemeLabel()}
    showChevron
    onPress={() => setShowThemeModal(true)}
  />
</View>

<ThemeSelectionModal
  visible={showThemeModal}
  onClose={() => setShowThemeModal(false)}
/>
```

## Visual Specs
- Row shows current theme as subtitle
- Tap opens theme selection modal

## Test Cases
```typescript
describe('Appearance Section', () => {
  it('shows current theme')
  it('opens theme modal on press')
})
```

## Completion Checklist
- [ ] Theme row renders
- [ ] Current theme shows
- [ ] Modal opens
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add appearance section to settings
```

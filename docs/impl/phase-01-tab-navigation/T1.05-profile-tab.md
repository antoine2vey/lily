# T1.05 - Profile Tab

Configure Profile tab with icon and screen.

## Status: Complete

## Depends On
- T1.01 (Tab Navigator Setup)

## Design Reference
- `general_app_settings` (Profile/Settings)

## Output Files
- Update `packages/app/src/navigation/MainTabNavigator.tsx`
- `packages/app/src/screens/ProfileScreen.tsx` (placeholder)

## Implementation
```typescript
<Tab.Screen
  name="Profile"
  component={ProfileScreen}
  options={{
    tabBarIcon: ({ focused }) => (
      <UserIcon
        color={focused ? '#5B8C5A' : '#9E9E9E'}
        size={24}
      />
    ),
    tabBarLabel: 'Profile',
  }}
/>
```

## Icon
- Use `user` or `user-circle` icon
- Active: #5B8C5A (green)
- Inactive: #9E9E9E (gray)

## Test Cases
```typescript
describe('Profile Tab', () => {
  it('shows user icon')
  it('highlights icon when active')
  it('shows "Profile" label')
  it('renders ProfileScreen content')
})
```

## Completion Checklist
- [x] Tab shows correct icon
- [x] Label reads "Profile"
- [x] ProfileScreen renders
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add profile tab to navigator
```

# T1.06 - Center FAB

Implement the center floating action button that opens Add Plant sheet.

## Status: Complete

## Depends On
- T0.02 (BottomTabBar)
- T1.01 (Tab Navigator Setup)

## Design Reference
- `home_dashboard` (green + button in center of tab bar)

## Output Files
- Update `packages/app/src/components/BottomTabBar.tsx`

## Implementation
```typescript
// In BottomTabBar component
<View style={styles.fabContainer}>
  <Pressable
    style={styles.fab}
    onPress={onFabPress}
    android_ripple={{ color: 'rgba(255,255,255,0.3)', borderless: true }}
  >
    <PlusIcon color="#FFFFFF" size={28} />
  </Pressable>
</View>

// Styles
const styles = StyleSheet.create({
  fabContainer: {
    position: 'absolute',
    top: -28,
    alignSelf: 'center',
  },
  fab: {
    width: 56,
    height: 56,
    borderRadius: 28,
    backgroundColor: '#5B8C5A',
    justifyContent: 'center',
    alignItems: 'center',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 4 },
    shadowOpacity: 0.3,
    shadowRadius: 4,
    elevation: 8,
  },
})
```

## Visual Specs
- Size: 56px diameter
- Color: #5B8C5A (primary green)
- Icon: white plus, 28px
- Elevation: lifted above tab bar
- Shadow: visible drop shadow

## Test Cases
```typescript
describe('Center FAB', () => {
  it('renders green circular button')
  it('shows plus icon in white')
  it('is elevated above tab bar')
  it('calls onFabPress when pressed')
  it('has press feedback (ripple/opacity)')
})
```

## Completion Checklist
- [x] FAB is green (#5B8C5A)
- [x] FAB is circular and elevated
- [x] Plus icon is white
- [x] Press opens Add Plant bottom sheet
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add center FAB to tab bar
```

# T1.04 - Care Tab

Configure Care/Tasks tab with icon and screen.

## Status: Complete

## Depends On
- T1.01 (Tab Navigator Setup)

## Design Reference
- `today's_care_tasks` (Care tab)

## Output Files
- Update `packages/app/src/navigation/MainTabNavigator.tsx`
- `packages/app/src/screens/CareScreen.tsx` (placeholder)

## Implementation
```typescript
<Tab.Screen
  name="Care"
  component={CareScreen}
  options={{
    tabBarIcon: ({ focused }) => (
      <CheckSquareIcon
        color={focused ? '#5B8C5A' : '#9E9E9E'}
        size={24}
      />
    ),
    tabBarLabel: 'Care',
  }}
/>
```

## Icon
- Use `check-square` or `clipboard-list` icon
- Active: #5B8C5A (green)
- Inactive: #9E9E9E (gray)

## Test Cases
```typescript
describe('Care Tab', () => {
  it('shows checklist icon')
  it('highlights icon when active')
  it('shows "Care" label')
  it('renders CareScreen content')
})
```

## Completion Checklist
- [x] Tab shows correct icon
- [x] Label reads "Care"
- [x] CareScreen renders
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add care tab to navigator
```

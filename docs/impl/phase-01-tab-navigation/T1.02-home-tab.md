# T1.02 - Home Tab

Configure Home tab with icon and screen.

## Status: Complete

## Depends On
- T1.01 (Tab Navigator Setup)

## Design Reference
- `home_dashboard` (Home tab in bottom nav)

## Output Files
- Update `packages/app/src/navigation/MainTabNavigator.tsx`

## Implementation
```typescript
<Tab.Screen
  name="Home"
  component={HomeScreen}
  options={{
    tabBarIcon: ({ focused }) => (
      <HomeIcon
        color={focused ? '#5B8C5A' : '#9E9E9E'}
        size={24}
      />
    ),
    tabBarLabel: 'Home',
  }}
/>
```

## Icon
- Use `home` icon from lucide-react-native or similar
- Active: #5B8C5A (green)
- Inactive: #9E9E9E (gray)

## Test Cases
```typescript
describe('Home Tab', () => {
  it('shows home icon')
  it('highlights icon when active')
  it('shows "Home" label')
  it('renders HomeScreen content')
})
```

## Completion Checklist
- [x] Tab shows correct icon
- [x] Active state is green
- [x] HomeScreen renders
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add home tab to navigator
```

# T1.03 - My Plants Tab

Configure My Plants tab with icon and screen.

## Status: Complete

## Depends On
- T1.01 (Tab Navigator Setup)

## Design Reference
- `plant_collection_grid` (My Plants tab)

## Output Files
- Update `packages/app/src/navigation/MainTabNavigator.tsx`
- `packages/app/src/screens/PlantsScreen.tsx` (placeholder)

## Implementation
```typescript
<Tab.Screen
  name="MyPlants"
  component={PlantsScreen}
  options={{
    tabBarIcon: ({ focused }) => (
      <LeafIcon
        color={focused ? '#5B8C5A' : '#9E9E9E'}
        size={24}
      />
    ),
    tabBarLabel: 'My Plants',
  }}
/>
```

## Icon
- Use `leaf` or `plant` icon
- Active: #5B8C5A (green)
- Inactive: #9E9E9E (gray)

## Test Cases
```typescript
describe('My Plants Tab', () => {
  it('shows plant/leaf icon')
  it('highlights icon when active')
  it('shows "My Plants" label')
  it('renders PlantsScreen content')
})
```

## Completion Checklist
- [x] Tab shows correct icon
- [x] Label reads "My Plants"
- [x] PlantsScreen renders
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add my plants tab to navigator
```

# T15.04 - Export My Data Button

Request data export.

## Status: Not Started

## Depends On
- T15.01 (Privacy Screen)

## Design Reference
- `privacy_and_data_settings` (Export My Data button)

## Output Files
- Update PrivacySettingsScreen

## Implementation
```typescript
const { mutate: exportData, isLoading } = useExportData()

<Button
  variant="outline"
  onPress={() => exportData()}
  loading={isLoading}
>
  Export My Data
</Button>

// Hook
export function useExportData() {
  return useMutation({
    mutationFn: async () => {
      const response = await api.user.exportData()
      // Download file or show success
      Alert.alert('Export Requested', 'You will receive an email with your data within 24 hours.')
    },
  })
}
```

## Visual Specs
- Button: green outline style
- Loading state while processing

## Test Cases
```typescript
describe('Export Data', () => {
  it('renders button')
  it('calls API on press')
  it('shows loading state')
  it('shows success message')
})
```

## Completion Checklist
- [ ] Button renders
- [ ] API called
- [ ] Success feedback
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add export data button
```

# T9.01 - Log Care Bottom Sheet

Bottom sheet form for logging care entry.

## Status: Not Started

## Depends On
- T0.01 (BottomSheet)

## Design Reference
- `log_care_entry`

## Output Files
- `packages/app/src/screens/log-care/LogCareSheet.tsx`
- `packages/app/src/screens/log-care/__tests__/LogCareSheet.test.tsx`

## Props Interface
```typescript
interface LogCareSheetProps {
  visible: boolean
  onClose: () => void
  defaultPlantId?: string
  onSuccess?: () => void
}
```

## Implementation
```typescript
export function LogCareSheet({ visible, onClose, defaultPlantId, onSuccess }: LogCareSheetProps) {
  const [plantId, setPlantId] = useState(defaultPlantId ?? '')
  const [careType, setCareType] = useState<CareType>('watered')
  const [date, setDate] = useState(new Date())
  const [time, setTime] = useState(new Date())
  const [notes, setNotes] = useState('')
  const [photo, setPhoto] = useState<string | null>(null)

  return (
    <BottomSheet visible={visible} onClose={onClose} title="Log Care">
      <ScrollView>
        {/* Form fields */}
      </ScrollView>
      <Button onPress={handleSave}>Save Log</Button>
    </BottomSheet>
  )
}
```

## Visual Specs
- Title: "Log Care" with X close
- Form fields: plant, type, date/time, notes, photo
- Save button: green, full width

## Test Cases
```typescript
describe('LogCareSheet', () => {
  it('renders when visible')
  it('pre-selects plant when defaultPlantId provided')
  it('has all form fields')
  it('calls onClose when X pressed')
})
```

## Completion Checklist
- [ ] Sheet renders
- [ ] All form fields present
- [ ] Close button works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add log care bottom sheet
```

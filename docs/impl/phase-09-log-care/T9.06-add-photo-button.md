# T9.06 - Add Photo Button

Optional photo attachment for care log.

## Status: Not Started

## Depends On
- T9.01 (Log Care Sheet)

## Design Reference
- `log_care_entry` (ADD PHOTO button)

## Output Files
- Update `packages/app/src/screens/log-care/LogCareSheet.tsx`

## Implementation
```typescript
const handleAddPhoto = async () => {
  const result = await ImagePicker.launchImageLibraryAsync({
    mediaTypes: ImagePicker.MediaTypeOptions.Images,
    allowsEditing: true,
    aspect: [4, 3],
    quality: 0.8,
  })

  if (!result.canceled) {
    setPhoto(result.assets[0].uri)
  }
}

<Pressable onPress={handleAddPhoto} style={styles.addPhotoButton}>
  {photo ? (
    <View style={styles.photoPreview}>
      <Image source={{ uri: photo }} style={styles.previewImage} />
      <Pressable onPress={() => setPhoto(null)} style={styles.removeButton}>
        <XIcon size={16} color="#FFF" />
      </Pressable>
    </View>
  ) : (
    <>
      <CameraIcon size={24} color="#9E9E9E" />
      <Text style={styles.addPhotoText}>ADD PHOTO</Text>
    </>
  )}
</Pressable>
```

## Visual Specs
- Button: dashed border, centered content
- No photo: camera icon + "ADD PHOTO"
- With photo: preview thumbnail + X remove
- Preview: 80px, rounded corners

## Test Cases
```typescript
describe('Add Photo', () => {
  it('shows add button when no photo')
  it('opens image picker on press')
  it('shows preview when photo selected')
  it('removes photo when X pressed')
})
```

## Completion Checklist
- [ ] Add button renders
- [ ] Picker opens
- [ ] Preview shows
- [ ] Remove works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add photo button to log care
```

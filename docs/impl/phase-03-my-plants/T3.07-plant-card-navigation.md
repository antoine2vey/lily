# T3.07 - Plant Card Navigation

Navigate to plant detail when card is pressed.

## Status: Complete

## Depends On
- T3.02 (Plant Card)

## Design Reference
- `plant_collection_grid` → `plant_detail_view`

## Output Files
- Update `packages/app/src/screens/plants/PlantsScreen.tsx`
- Update `packages/app/src/screens/plants/components/PlantCard.tsx`

## Implementation
```typescript
// In PlantsScreen
const navigation = useNavigation()

const handlePlantPress = (plantId: string) => {
  navigation.navigate('PlantDetail', { plantId })
}

<PlantCard
  plant={item}
  onPress={handlePlantPress}
/>

// In PlantCard
<Pressable onPress={() => onPress(plant.id)} style={styles.card}>
  {/* card content */}
</Pressable>
```

## Navigation Setup
Add to navigation types:
```typescript
type RootStackParamList = {
  // ...
  PlantDetail: { plantId: string }
}
```

## Test Cases
```typescript
describe('Plant Card Navigation', () => {
  it('navigates to PlantDetail when card pressed')
  it('passes plantId as route param')
})
```

## Completion Checklist
- [x] Card is pressable
- [x] Navigation fires on press
- [x] Plant ID passed correctly
- [x] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add plant card navigation to detail
```

# T3.08 - Empty Collection State

Empty state when user has no plants in collection.

## Status: Not Started

## Depends On
- T0.12 (EmptyState)
- T3.01 (Plants Grid Screen)

## Design Reference
- Similar to `home_empty_state`

## Output Files
- Update `packages/app/src/screens/plants/PlantsScreen.tsx`

## Implementation
```typescript
export function PlantsScreen() {
  const { data: plants, isLoading } = usePlants()
  const [showAddPlant, setShowAddPlant] = useState(false)

  if (!isLoading && plants?.length === 0) {
    return (
      <SafeAreaView style={styles.container}>
        <View style={styles.header}>
          <Text style={styles.title}>My Plants</Text>
        </View>
        <EmptyState
          illustration="plant"
          title="No plants yet"
          description="Start building your collection by adding your first plant"
          action={{
            label: "Add Plant",
            onPress: () => setShowAddPlant(true)
          }}
        />
        <AddPlantOptionsSheet
          visible={showAddPlant}
          onClose={() => setShowAddPlant(false)}
        />
      </SafeAreaView>
    )
  }

  // ... grid content
}
```

## Visual Specs
- Same as home empty state
- Title: "No plants yet"
- Description: "Start building your collection by adding your first plant"

## Test Cases
```typescript
describe('Empty Collection State', () => {
  it('renders empty state when no plants')
  it('still shows header')
  it('shows add plant CTA')
  it('opens add plant sheet when CTA pressed')
})
```

## Completion Checklist
- [ ] Empty state shows when plants.length === 0
- [ ] Header remains visible
- [ ] CTA opens add plant flow
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add empty state to plants screen
```

# T3.03 - Filter Chips

Horizontal filter chips for plant collection.

## Status: Not Started

## Depends On
- T0.10 (Chip)

## Design Reference
- `plant_collection_grid` (All, Healthy, Needs Attention chips)

## Output Files
- `packages/app/src/screens/plants/components/PlantFilters.tsx`
- `packages/app/src/screens/plants/components/__tests__/PlantFilters.test.tsx`

## Props Interface
```typescript
interface PlantFiltersProps {
  selectedFilter: 'all' | 'healthy' | 'attention'
  onFilterChange: (filter: 'all' | 'healthy' | 'attention') => void
  counts: {
    all: number
    healthy: number
    attention: number
  }
}
```

## Implementation
```typescript
const FILTERS = [
  { key: 'all', label: 'All' },
  { key: 'healthy', label: 'Healthy' },
  { key: 'attention', label: 'Needs Attention' },
]

export function PlantFilters({ selectedFilter, onFilterChange, counts }: PlantFiltersProps) {
  return (
    <ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.container}>
      {FILTERS.map(({ key, label }) => (
        <Chip
          key={key}
          label={`${label} (${counts[key]})`}
          selected={selectedFilter === key}
          onPress={() => onFilterChange(key)}
        />
      ))}
    </ScrollView>
  )
}
```

## Visual Specs
- Horizontal scroll, no scrollbar
- Chips: 8px gap between
- Container padding: 16px horizontal
- Container margin: 12px vertical

## Test Cases
```typescript
describe('PlantFilters', () => {
  it('renders all filter options')
  it('shows count in chip label')
  it('highlights selected filter')
  it('calls onFilterChange when chip pressed')
})
```

## Completion Checklist
- [ ] All 3 filter chips render
- [ ] Counts show in labels
- [ ] Selected state is visually distinct
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add plant filter chips
```

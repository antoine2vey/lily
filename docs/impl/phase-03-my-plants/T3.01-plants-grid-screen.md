# T3.01 - Plants Grid Screen Shell

Basic plants collection screen with header and grid layout.

## Status: Not Started

## Depends On
- T1.03 (My Plants Tab)

## Design Reference
- `plant_collection_grid`

## Output Files
- `packages/app/src/screens/plants/PlantsScreen.tsx`
- `packages/app/src/screens/plants/__tests__/PlantsScreen.test.tsx`

## Implementation
```typescript
export function PlantsScreen() {
  const { data: plants, isLoading } = usePlants()

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <Text style={styles.title}>My Plants</Text>
        <View style={styles.headerActions}>
          <SearchButton />
          <ViewToggleButton />
        </View>
      </View>
      <FilterChips />
      <FlatList
        data={plants}
        numColumns={2}
        renderItem={({ item }) => <PlantCard plant={item} />}
        keyExtractor={(item) => item.id}
        contentContainerStyle={styles.grid}
      />
    </SafeAreaView>
  )
}
```

## Visual Specs
- Title: "My Plants", 28px, bold
- Grid: 2 columns, 12px gap
- Padding: 16px horizontal

## Test Cases
```typescript
describe('PlantsScreen', () => {
  it('renders "My Plants" title')
  it('renders search icon')
  it('renders view toggle icon')
  it('renders plants in 2-column grid')
  it('shows loading state while fetching')
})
```

## Completion Checklist
- [ ] Header with title and icons
- [ ] Grid layout with 2 columns
- [ ] Loading state
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add plants grid screen shell
```

# T5.10 - Create Plant API Integration

API hook for creating a new plant.

## Status: Not Started

## Depends On
- T5.08 (Manual Add Step 3)

## Design Reference
- N/A (API integration)

## Output Files
- `packages/app/src/hooks/useCreatePlant.ts`
- `packages/app/src/hooks/__tests__/useCreatePlant.test.ts`

## Implementation
```typescript
interface CreatePlantInput {
  name: string
  species?: string
  category?: string
  imageUri?: string
  wateringFrequency: number
  fertilizingFrequency: number
  lightNeeds?: number
  waterNeeds?: number
  humidityNeeds?: number
  petSafe?: boolean
  notificationsEnabled?: boolean
  notes?: string
}

export function useCreatePlant() {
  const queryClient = useQueryClient()

  return useMutation({
    mutationFn: async (input: CreatePlantInput) => {
      // Upload image first if provided
      let imageUrl: string | undefined
      if (input.imageUri) {
        imageUrl = await uploadPlantImage(input.imageUri)
      }

      // Create plant via API
      const plant = await api.plants.create({
        ...input,
        imageUrl,
      })

      return plant
    },
    onSuccess: () => {
      // Invalidate plants list to refetch
      queryClient.invalidateQueries(['plants'])
    },
  })
}

async function uploadPlantImage(uri: string): Promise<string> {
  const formData = new FormData()
  formData.append('file', {
    uri,
    type: 'image/jpeg',
    name: 'plant.jpg',
  })

  const response = await api.plants.uploadPhoto(formData)
  return response.url
}
```

## Test Cases
```typescript
describe('useCreatePlant', () => {
  it('uploads image when imageUri provided')
  it('creates plant with all fields')
  it('returns created plant')
  it('invalidates plants query on success')
  it('handles image upload error')
  it('handles API error')
})
```

## Completion Checklist
- [ ] Image upload works
- [ ] Plant creation API call
- [ ] Query invalidation
- [ ] Error handling
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add useCreatePlant hook
```

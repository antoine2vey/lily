# T12.02 - Care Reminders Section

Toggles for watering and fertilization reminders.

## Status: Not Started

## Depends On
- T0.07 (ToggleRow)
- T12.01 (Notification Settings Screen)

## Design Reference
- `notification_settings` (Care Reminders section)

## Output Files
- Update NotificationSettingsScreen

## Implementation
```typescript
<View style={styles.section}>
  <Text style={styles.sectionTitle}>Care Reminders</Text>
  <ToggleRow
    label="Watering Reminders"
    description="Get notified when plants need water"
    value={settings.wateringReminders}
    onValueChange={(v) => updateSetting('wateringReminders', v)}
  />
  <ToggleRow
    label="Fertilization Reminders"
    description="Get notified when plants need feeding"
    value={settings.fertilizationReminders}
    onValueChange={(v) => updateSetting('fertilizationReminders', v)}
  />
</View>
```

## Visual Specs
- Section title: 18px, semibold
- Toggle rows: standard styling

## Test Cases
```typescript
describe('Care Reminders Section', () => {
  it('renders section title')
  it('renders watering toggle')
  it('renders fertilization toggle')
  it('updates setting on toggle')
})
```

## Completion Checklist
- [ ] Section renders
- [ ] Toggles work
- [ ] Settings update
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add care reminders settings section
```

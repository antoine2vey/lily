# T12.04 - Updates & Alerts Section

Toggles for achievements, tips, and product updates.

## Status: Not Started

## Depends On
- T0.07 (ToggleRow)
- T12.01 (Notification Settings Screen)

## Design Reference
- `notification_settings` (Updates & Alerts section)

## Output Files
- Update NotificationSettingsScreen

## Implementation
```typescript
<View style={styles.section}>
  <Text style={styles.sectionTitle}>Updates & Alerts</Text>
  <ToggleRow
    label="Achievements"
    description="Get notified when you unlock achievements"
    value={settings.achievementAlerts}
    onValueChange={(v) => updateSetting('achievementAlerts', v)}
  />
  <ToggleRow
    label="Plant Care Tips"
    description="Receive helpful tips for your plants"
    value={settings.careTips}
    onValueChange={(v) => updateSetting('careTips', v)}
  />
  <ToggleRow
    label="Product Updates"
    description="Learn about new features"
    value={settings.productUpdates}
    onValueChange={(v) => updateSetting('productUpdates', v)}
  />
</View>
```

## Test Cases
```typescript
describe('Updates & Alerts Section', () => {
  it('renders all three toggles')
  it('updates settings on toggle')
})
```

## Completion Checklist
- [ ] All toggles render
- [ ] Settings update
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add updates alerts settings section
```

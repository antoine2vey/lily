# T12.01 - Notification Settings Screen

Settings screen for notification preferences.

## Status: Not Started

## Depends On
- T14.06 (Settings navigation)

## Design Reference
- `notification_settings`

## Output Files
- `packages/app/src/screens/settings/NotificationSettingsScreen.tsx`
- `packages/app/src/screens/settings/__tests__/NotificationSettingsScreen.test.tsx`

## Implementation
```typescript
export function NotificationSettingsScreen() {
  const { data: settings, isLoading } = useNotificationSettings()

  if (isLoading) return <LoadingScreen />

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <BackButton />
        <Text style={styles.title}>Notifications</Text>
      </View>
      <ScrollView>
        {/* Settings sections */}
      </ScrollView>
    </SafeAreaView>
  )
}
```

## Visual Specs
- Header: "Notifications" title
- Sections: Care Reminders, Updates & Alerts, Do Not Disturb

## Test Cases
```typescript
describe('NotificationSettingsScreen', () => {
  it('renders header')
  it('fetches current settings')
  it('shows loading state')
  it('renders all sections')
})
```

## Completion Checklist
- [ ] Header renders
- [ ] Settings load
- [ ] Sections display
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add notification settings screen
```

# T2.02 - Hydration Time Card

Card showing plants that need water today.

## Status: Complete

## Depends On
- T0.03 (Card)
- T2.01 (Home Screen Shell)

## Design Reference
- `home_dashboard` (Hydration Time card with plant thumbnails)

## Output Files
- `packages/app/src/screens/home/components/HydrationCard.tsx`
- `packages/app/src/screens/home/components/__tests__/HydrationCard.test.tsx`

## Props Interface
```typescript
interface HydrationCardProps {
  plants: Array<{
    id: string
    name: string
    imageUrl?: string
  }>
  onWaterAll: () => void
  onPlantPress: (plantId: string) => void
}
```

## Implementation Details
- Card with light green background (#F0F7F0)
- Water droplet icon in top-right corner
- Title: "Hydration Time"
- Subtitle: "X plants need water today"
- Horizontal row of circular plant thumbnails (max 3)
- "Water All" button (green, full width, with checkmark icon)

## Visual Specs
- Card background: #F0F7F0
- Plant thumbnails: 56px circles
- Title: 18px, semibold, #1A1A1A
- Subtitle: 14px, regular, #757575
- Button: 48px height, #5B8C5A

## Test Cases
```typescript
describe('HydrationCard', () => {
  it('renders title "Hydration Time"')
  it('renders plant count in subtitle')
  it('renders up to 3 plant thumbnails')
  it('shows "+X" indicator when more than 3 plants')
  it('calls onWaterAll when button pressed')
  it('calls onPlantPress when plant thumbnail pressed')
  it('returns null when no plants need water')
})
```

## Completion Checklist
- [ ] Card matches design (rounded, light green bg)
- [ ] Plant thumbnails are circular
- [ ] "Water All" button works
- [ ] Hidden when 0 plants need water
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add hydration time card to home
```

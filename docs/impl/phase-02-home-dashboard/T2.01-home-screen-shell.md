# T2.01 - Home Screen Shell

Basic home screen structure with header.

## Status: Complete

## Depends On
- T0.09 (Avatar)
- T1.02 (Home Tab)

## Design Reference
- `home_dashboard` (top section with "Good morning, Alex")

## Output Files
- `packages/app/src/screens/home/HomeScreen.tsx`
- `packages/app/src/screens/home/__tests__/HomeScreen.test.tsx`

## Implementation
```typescript
export function HomeScreen() {
  return (
    <SafeAreaView style={styles.container}>
      <ScrollView contentContainerStyle={styles.content}>
        <HomeHeader />
        {/* Content sections added in subsequent tasks */}
      </ScrollView>
    </SafeAreaView>
  )
}

function HomeHeader() {
  const { user } = useAuth()
  const greeting = getGreeting() // Based on time of day

  return (
    <View style={styles.header}>
      <View>
        <Text style={styles.greeting}>{greeting},</Text>
        <Text style={styles.username}>{user?.username} ☀️</Text>
      </View>
      <View style={styles.headerRight}>
        <Avatar source={{ uri: user?.avatarUrl }} name={user?.username} size="md" />
        <NotificationBell />
      </View>
    </View>
  )
}

function getGreeting(): string {
  const hour = new Date().getHours()
  if (hour < 12) return 'Good morning'
  if (hour < 17) return 'Good afternoon'
  return 'Good evening'
}
```

## Visual Specs
- Greeting: 16px, regular, #757575
- Username: 28px, bold, #1A1A1A
- Header padding: 16px horizontal, 24px top
- Background: #F8FAF8

## Test Cases
```typescript
describe('HomeScreen', () => {
  it('renders greeting based on time of day')
  it('renders username from auth context')
  it('renders user avatar')
  it('renders notification bell')
  it('renders ScrollView for content')
})
```

## Completion Checklist
- [ ] Greeting changes based on time
- [ ] Username displays from auth context
- [ ] Avatar and bell render in header right
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add home screen shell with header
```

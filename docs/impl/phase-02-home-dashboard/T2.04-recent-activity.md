# T2.04 - Recent Activity Section

List of recent plant care events.

## Status: Complete

## Depends On
- T0.04 (SectionHeader)
- T2.01 (Home Screen Shell)

## Design Reference
- `home_dashboard` (Recent Activity section)

## Output Files
- `packages/app/src/screens/home/components/RecentActivity.tsx`
- `packages/app/src/screens/home/components/__tests__/RecentActivity.test.tsx`

## Props Interface
```typescript
interface RecentActivityProps {
  activities: Array<{
    id: string
    type: 'watered' | 'fertilized' | 'added' | 'moved' | 'misted' | 'pruned'
    plantName: string
    timestamp: Date
    plantImageUrl?: string
  }>
  onSeeAll: () => void
  onActivityPress: (activityId: string) => void
}
```

## Implementation
```typescript
const ACTIVITY_ICONS = {
  watered: { icon: DropletIcon, color: '#2196F3' },
  fertilized: { icon: SparklesIcon, color: '#FF9800' },
  added: { icon: PlusCircleIcon, color: '#5B8C5A' },
  moved: { icon: SunIcon, color: '#FFC107' },
  misted: { icon: CloudIcon, color: '#90CAF9' },
  pruned: { icon: ScissorsIcon, color: '#E91E63' },
}

const ACTIVITY_LABELS = {
  watered: 'watered',
  fertilized: 'fertilized',
  added: 'New plant added:',
  moved: 'Moved',
  misted: 'misted',
  pruned: 'pruned',
}
```

## Visual Specs
- Activity row: 64px height, white background, 12px border-radius
- Icon: 40px circle with light color variant background
- Title: 16px, medium, #1A1A1A (e.g., "Fiddle Leaf Fig misted")
- Timestamp: 14px, regular, #9E9E9E (e.g., "2h ago")

## Test Cases
```typescript
describe('RecentActivity', () => {
  it('renders section header with "See All"')
  it('renders activity items')
  it('shows correct icon per activity type')
  it('shows relative time (2h ago, Yesterday)')
  it('calls onSeeAll when header action pressed')
  it('calls onActivityPress when item pressed')
  it('renders empty message when no activities')
})
```

## Completion Checklist
- [ ] Activities show with correct icons
- [ ] Time is relative format
- [ ] "See All" navigates to full history
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add recent activity to home
```

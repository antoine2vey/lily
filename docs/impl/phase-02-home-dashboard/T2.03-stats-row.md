# T2.03 - Stats Row

Three stat boxes showing total, healthy, and attention counts.

## Status: Complete

## Depends On
- T2.01 (Home Screen Shell)

## Design Reference
- `home_dashboard` (12 TOTAL, 10 HEALTHY, 2 ATTENTION)

## Output Files
- `packages/app/src/screens/home/components/StatsRow.tsx`
- `packages/app/src/screens/home/components/__tests__/StatsRow.test.tsx`

## Props Interface
```typescript
interface StatsRowProps {
  total: number
  healthy: number
  attention: number
}
```

## Implementation
```typescript
export function StatsRow({ total, healthy, attention }: StatsRowProps) {
  return (
    <View style={styles.container}>
      <StatBox value={total} label="TOTAL" />
      <StatBox value={healthy} label="HEALTHY" />
      <StatBox
        value={attention}
        label="ATTENTION"
        variant="warning"
        icon={<AlertTriangleIcon size={16} color="#F57C00" />}
      />
    </View>
  )
}
```

## Visual Specs
- Container: horizontal row, 8px gap
- Each box: flex-1, white background, 16px padding, 12px border-radius
- Value: 32px, bold, #1A1A1A
- Label: 10px, medium, #757575, uppercase, letter-spacing 0.5px
- Attention variant: orange icon, value color #F57C00 when > 0

## Test Cases
```typescript
describe('StatsRow', () => {
  it('renders total count')
  it('renders healthy count')
  it('renders attention count')
  it('shows warning icon for attention when > 0')
  it('uses orange color for attention value when > 0')
  it('uses normal color for attention when = 0')
})
```

## Completion Checklist
- [ ] Three boxes are equal width
- [ ] Numbers are large and bold
- [ ] Labels are small caps
- [ ] Attention box highlighted when > 0
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add stats row to home
```

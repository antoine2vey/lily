# T2.06 - Notification Bell

Bell icon with unread count badge.

## Status: Not Started

## Depends On
- None (standalone component)

## Design Reference
- `home_dashboard` (bell icon in header)

## Output Files
- `packages/app/src/components/NotificationBell.tsx`
- `packages/app/src/components/__tests__/NotificationBell.test.tsx`

## Props Interface
```typescript
interface NotificationBellProps {
  unreadCount?: number
  onPress: () => void
}
```

## Implementation
```typescript
export function NotificationBell({ unreadCount = 0, onPress }: NotificationBellProps) {
  const displayCount = unreadCount > 9 ? '9+' : unreadCount.toString()

  return (
    <Pressable onPress={onPress} style={styles.container}>
      <BellIcon size={24} color="#1A1A1A" />
      {unreadCount > 0 && (
        <View style={styles.badge}>
          <Text style={styles.badgeText}>{displayCount}</Text>
        </View>
      )}
    </Pressable>
  )
}
```

## Visual Specs
- Icon: 24px, #1A1A1A
- Badge: 18px min-width, 18px height, circular
- Badge background: #E8997E (coral)
- Badge text: 10px, bold, #FFFFFF
- Badge position: top-right, offset by -4px

## Test Cases
```typescript
describe('NotificationBell', () => {
  it('renders bell icon')
  it('shows badge when unreadCount > 0')
  it('hides badge when unreadCount = 0')
  it('shows "9+" when count > 9')
  it('calls onPress when pressed')
})
```

## Completion Checklist
- [ ] Bell icon matches design
- [ ] Badge is positioned top-right
- [ ] Badge is coral color
- [ ] Navigates to notifications screen
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add NotificationBell component
```

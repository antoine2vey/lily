# T13.08 - Achievement Tap → Modal

Open correct modal when card tapped.

## Status: Not Started

## Depends On
- T13.06 (Unlocked Modal)
- T13.07 (Locked Modal)

## Design Reference
- `achievements_gallery_1` tap interaction

## Output Files
- Update `packages/app/src/screens/achievements/AchievementsScreen.tsx`

## Implementation
```typescript
const [selectedAchievement, setSelectedAchievement] = useState<Achievement | null>(null)

const handleAchievementPress = (achievement: Achievement) => {
  setSelectedAchievement(achievement)
}

{selectedAchievement && (
  selectedAchievement.unlockedAt ? (
    <UnlockedAchievementModal
      visible={true}
      achievement={selectedAchievement}
      onClose={() => setSelectedAchievement(null)}
    />
  ) : (
    <LockedAchievementModal
      visible={true}
      achievement={selectedAchievement}
      onClose={() => setSelectedAchievement(null)}
    />
  )
)}
```

## Test Cases
```typescript
describe('Achievement Tap', () => {
  it('opens unlocked modal for unlocked achievement')
  it('opens locked modal for locked achievement')
  it('closes modal on close press')
})
```

## Completion Checklist
- [ ] Correct modal opens
- [ ] Modal closes
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add achievement tap to modal
```

# T13.03 - Achievement Category Section

Section grouping achievements by category.

## Status: Not Started

## Depends On
- T13.01 (Achievements Screen)

## Design Reference
- `achievements_gallery_1` (Getting Started, Plant Collector sections)

## Output Files
- `packages/app/src/screens/achievements/components/AchievementSection.tsx`

## Implementation
```typescript
interface AchievementSectionProps {
  title: string
  achievements: Achievement[]
  onAchievementPress: (achievement: Achievement) => void
}

export function AchievementSection({ title, achievements, onAchievementPress }: AchievementSectionProps) {
  return (
    <View style={styles.section}>
      <Text style={styles.sectionTitle}>{title}</Text>
      <View style={styles.grid}>
        {achievements.map((achievement) => (
          <AchievementCard
            key={achievement.id}
            achievement={achievement}
            onPress={() => onAchievementPress(achievement)}
          />
        ))}
      </View>
    </View>
  )
}
```

## Visual Specs
- Section title: 18px, semibold, uppercase
- Grid: 3 columns

## Test Cases
```typescript
describe('AchievementSection', () => {
  it('renders section title')
  it('renders achievement cards in grid')
  it('calls onPress when card tapped')
})
```

## Completion Checklist
- [ ] Title renders
- [ ] Grid layout
- [ ] Cards display
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add achievement category section
```

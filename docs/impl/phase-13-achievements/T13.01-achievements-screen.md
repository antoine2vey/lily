# T13.01 - Achievements Screen Shell

Main achievements gallery screen.

## Status: Not Started

## Depends On
- Navigation setup

## Design Reference
- `achievements_gallery_1`

## Output Files
- `packages/app/src/screens/achievements/AchievementsScreen.tsx`
- `packages/app/src/screens/achievements/__tests__/AchievementsScreen.test.tsx`

## Implementation
```typescript
export function AchievementsScreen() {
  const { data: achievements, isLoading } = useAchievements()

  return (
    <SafeAreaView style={styles.container}>
      <View style={styles.header}>
        <BackButton />
        <Text style={styles.title}>Achievements</Text>
      </View>
      <ScrollView>
        <LevelHeader level={achievements?.level} />
        {/* Achievement sections */}
      </ScrollView>
    </SafeAreaView>
  )
}
```

## Visual Specs
- Header: "Achievements" title
- Level badge: current level indicator

## Test Cases
```typescript
describe('AchievementsScreen', () => {
  it('renders header')
  it('fetches achievements')
  it('shows loading state')
})
```

## Completion Checklist
- [ ] Header renders
- [ ] Data loads
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add achievements screen shell
```

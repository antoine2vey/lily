# T13.06 - Unlocked Detail Modal

Modal showing unlocked achievement details.

## Status: Not Started

## Depends On
- T0.06 (Modal base)
- T13.04 (Achievement Card)

## Design Reference
- `unlocked_achievement_detail`

## Output Files
- `packages/app/src/screens/achievements/components/UnlockedAchievementModal.tsx`

## Implementation
```typescript
export function UnlockedAchievementModal({ visible, achievement, onClose }: UnlockedAchievementModalProps) {
  return (
    <Modal visible={visible} onClose={onClose}>
      <View style={styles.content}>
        <View style={styles.iconContainer}>
          <AchievementIcon type={achievement.icon} size={64} />
        </View>
        <Text style={styles.name}>{achievement.name}</Text>
        <Text style={styles.description}>{achievement.description}</Text>
        <Text style={styles.unlockedDate}>
          Unlocked on {formatDate(achievement.unlockedAt)}
        </Text>
        <Button variant="outline" onPress={handleShare}>
          Share Achievement
        </Button>
        <TextButton onPress={onClose}>Close</TextButton>
      </View>
    </Modal>
  )
}
```

## Visual Specs
- Icon: 64px, centered, with sparkles
- Name: 24px, bold
- Description: 14px, #757575, centered
- Date: 12px, green, with calendar icon
- Share button: outline style

## Test Cases
```typescript
describe('UnlockedAchievementModal', () => {
  it('renders achievement icon')
  it('renders name and description')
  it('shows unlocked date')
  it('has share button')
  it('closes on Close press')
})
```

## Completion Checklist
- [ ] Icon displays
- [ ] Details show
- [ ] Share works
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add unlocked achievement modal
```

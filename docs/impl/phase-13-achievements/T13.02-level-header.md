# T13.02 - Current Status Header

Level badge and overall progress.

## Status: Not Started

## Depends On
- T13.01 (Achievements Screen)
- T0.08 (ProgressBar)

## Design Reference
- `achievements_gallery_1` (level badge at top)

## Output Files
- `packages/app/src/screens/achievements/components/LevelHeader.tsx`

## Implementation
```typescript
interface LevelHeaderProps {
  level: number
  totalUnlocked: number
  totalAchievements: number
}

export function LevelHeader({ level, totalUnlocked, totalAchievements }: LevelHeaderProps) {
  const progress = totalUnlocked / totalAchievements

  return (
    <View style={styles.container}>
      <View style={styles.levelBadge}>
        <Text style={styles.levelText}>Level {level}</Text>
      </View>
      <Text style={styles.progressText}>
        {totalUnlocked} of {totalAchievements} achievements unlocked
      </Text>
      <ProgressBar progress={progress} />
    </View>
  )
}
```

## Visual Specs
- Level badge: green pill, white text
- Progress text: 14px, #757575
- Progress bar: below text

## Test Cases
```typescript
describe('LevelHeader', () => {
  it('renders level badge')
  it('shows progress text')
  it('shows progress bar')
})
```

## Completion Checklist
- [ ] Level displays
- [ ] Progress shows
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add level header to achievements
```

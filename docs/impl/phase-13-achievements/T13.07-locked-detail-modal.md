# T13.07 - Locked Detail Modal

Modal showing locked achievement with progress.

## Status: Not Started

## Depends On
- T0.08 (ProgressBar)
- T13.05 (Locked Card)

## Design Reference
- `locked_achievement_detail`

## Output Files
- `packages/app/src/screens/achievements/components/LockedAchievementModal.tsx`

## Implementation
```typescript
export function LockedAchievementModal({ visible, achievement, onClose }: LockedAchievementModalProps) {
  return (
    <Modal visible={visible} onClose={onClose}>
      <View style={styles.content}>
        <View style={styles.iconContainer}>
          <AchievementIcon type={achievement.icon} locked size={64} />
          <LockIcon style={styles.lockOverlay} />
        </View>
        <Text style={styles.name}>{achievement.name}</Text>
        <Text style={styles.description}>{achievement.description}</Text>

        <View style={styles.progressSection}>
          <Text style={styles.progressLabel}>Progress</Text>
          <Text style={styles.progressValue}>
            {achievement.progress} of {achievement.target} {achievement.unit}
          </Text>
          <ProgressBar progress={achievement.progress / achievement.target} />
        </View>

        <View style={styles.hintCard}>
          <LightbulbIcon />
          <Text style={styles.hintText}>{achievement.hint}</Text>
        </View>

        <Button onPress={handleAction}>Add Plant</Button>
      </View>
    </Modal>
  )
}
```

## Visual Specs
- Icon: grayscale, lock overlay
- Progress section: label + value + bar
- Hint card: yellow background, lightbulb icon
- CTA button: action to help progress

## Test Cases
```typescript
describe('LockedAchievementModal', () => {
  it('renders grayscale icon with lock')
  it('shows progress bar')
  it('shows hint card')
  it('has action button')
})
```

## Completion Checklist
- [ ] Locked styling
- [ ] Progress shows
- [ ] Hint displays
- [ ] All tests pass

## Ralph Loop Completion
Output `<promise>TASK COMPLETE</promise>` when all checklist items are done and tests pass.

## Commit Message
```
feat(app): add locked achievement modal
```
